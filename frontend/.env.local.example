# Copy to .env.local

# Base URL of the Express REST API.
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Base URL of the Socket.IO server (Express same port).
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
